# LGS Design System (Python)

Shared **colors** and **typography** for Python projects.

## Usage

### Colors
```python
import colors
print(colors.get("apple_green"))
print(colors.palette("primary"))
print(colors.rgb("tangerine"))
